<?xml version="1.0" encoding="utf-8"?>
<!-- @author  phi@gressly.eu
     @webpage http://igeeks.gressly.eu
     versionCode / versionName / SDK levels are declared in app/build.gradle.
     The package name is declared as the `namespace` in app/build.gradle.
  -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:logo="@drawable/ic_launcher"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.StartMainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.SlideShowActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="false"
            android:label="@string/title_activity_slide_show"
            android:theme="@style/FullscreenTheme" />

        <activity
            android:name=".activities.FinishScreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="false"
            android:label="@string/title_activity_finished"
            android:theme="@style/FullscreenTheme" />
    </application>

</manifest>
